Upload the contents of this deploy folder to GitHub Pages or any static web host.

Required public entry point:
index.html

After deployment, the project URL should open the dashboard directly.
Use that public URL at the beginning of the report.
